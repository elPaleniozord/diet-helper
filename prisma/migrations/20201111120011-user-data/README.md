# Migration `20201111120011-user-data`

This migration has been generated by elPaleniozord at 11/11/2020, 1:00:11 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."Role" AS ENUM ('USER', 'PRO_USER', 'ADMIN')

CREATE TYPE "public"."Gender" AS ENUM ('MALE', 'FEMALE')

ALTER TABLE "public"."users" ADD COLUMN "role" "Role"  NOT NULL DEFAULT E'USER'

CREATE TABLE "public"."users_data" (
"id" SERIAL,
"userId" text   NOT NULL ,
"height" integer   NOT NULL ,
"weight" integer   NOT NULL ,
"age" integer   NOT NULL ,
"gender" "Gender"  NOT NULL ,
"goal" text   NOT NULL ,
"kcal" integer   NOT NULL ,
"prot" integer   NOT NULL ,
"carb" integer   NOT NULL ,
"fats" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."logs" (
"id" SERIAL,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"weight" integer   NOT NULL ,
"userId" integer   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."favorites" (
"id" SERIAL,
"userId" integer   ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "users_data_userId_unique" ON "public"."users_data"("userId")

ALTER TABLE "public"."users_data" ADD FOREIGN KEY("userId")REFERENCES "public"."users"("email") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."logs" ADD FOREIGN KEY("userId")REFERENCES "public"."users"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."favorites" ADD FOREIGN KEY("userId")REFERENCES "public"."users"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201108203914-access-token-expiration-fix..20201111120011-user-data
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -49,8 +49,12 @@
   emailVerified DateTime? @map(name: "email_verified")
   image         String?
   createdAt     DateTime  @default(now()) @map(name: "created_at")
   updatedAt     DateTime  @default(now()) @map(name: "updated_at")
+  role          Role      @default(USER)
+  userData      UserData?  
+  logs          Log[]
+  favorites     Favorite[] 
   @@map(name: "users")
 }
@@ -63,4 +67,46 @@
   updatedAt  DateTime @default(now()) @map(name: "updated_at")
   @@map(name: "verification_requests")
 }
+
+model UserData {
+  id Int @default(autoincrement()) @id
+  user User @relation(fields: [userId], references: [email])
+  userId String
+  height Int
+  weight Int
+  age Int
+  gender Gender
+  goal String
+  kcal Int
+  prot Int
+  carb Int
+  fats Int
+
+  @@map(name: "users_data")
+}
+
+model Log {
+  id Int @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  weight Int
+
+  @@map(name: "logs")
+}
+
+model Favorite {
+  id Int @default(autoincrement()) @id
+
+  @@map(name: "favorites")
+}
+
+enum Role {
+  USER
+  PRO_USER
+  ADMIN
+}
+
+enum Gender {
+  MALE
+  FEMALE
+}
```


